# Source before running media-util tools for PY4E:
#   source /Users/csev/htdocs/py4e/media.env

# ---------------------------------------------------------------------------
# Per-course settings (edit these when copying for another course)
# ---------------------------------------------------------------------------
export COURSE_ROOT=/Users/csev/htdocs/py4e
export MEDIA_ROOT=/Users/csev/Desktop/teach/py4e-media
export WHISPER_ROOT=/Users/csev/htdocs/py4e/whisper
export YOUTUBE_DIR=/Users/csev/htdocs/py4e/youtube
export YOUTUBE_PLAYLIST='https://www.youtube.com/playlist?list=PLlRFEj9H3Oj7Bp8-DfGpfAfDBiblRfl5p'
export COURSE_HINT='Python for Everybody, PY4E, Python, Dr. Chuck, Chuck Severance'
export TITLE_PREFIX='PY'
export EXTRA_TAGS='py4e, py4e-lecture'
export EXTRA_DESCRIPTION='For more materials, auto graders, and more courses, please see www.py4e.com and www.masterprogrammer.com.'

# Optional Kaltura / MiVideo (UMSI ALI) — uncomment when playlist is ready
# export KALTURA_PARTNER_ID=1038472
# export KALTURA_SERVICE_URL='https://www.kaltura.com'
# export KALTURA_PLAYLIST_ID=
# export KALTURA_PLAYLIST_URL=
# export KALTURA_UICONF_ID=58045402
# export KALTURA_EMBED='https://cdnapisec.kaltura.com/p/1038472/embedPlaykitJs/uiconf_id/58045402?iframeembed=true&entry_id={id}'
# export KALTURA_TAB=
# Admin secret: set env OR put one line in ~/.ssh/kaltura_admin_secret
# export KALTURA_ADMIN_SECRET='…'
# export KALTURA_SECRET_FILE=$HOME/.ssh/kaltura_admin_secret

# Optional per-course overrides:
# export MODEL=$HOME/models/ggml-medium.bin
# export OLLAMA_MODEL=qwen3:4b

# ---------------------------------------------------------------------------
# Shared boilerplate (usually leave alone)
# ---------------------------------------------------------------------------
PWD_NOW="$(pwd -P)"

case "$PWD_NOW" in
  "$COURSE_ROOT"|"$COURSE_ROOT"/*) ;;
  *)
    echo "ERROR: media.env is for this course; pwd is $PWD_NOW" >&2
    echo "       expected under $COURSE_ROOT" >&2
    return 1 2>/dev/null || exit 1
    ;;
esac

export MEDIA_UTIL=/Users/csev/htdocs/media-util
case ":$PATH:" in
  *":$MEDIA_UTIL/bin:"*) ;;
  *) export PATH="$MEDIA_UTIL/bin:$PATH" ;;
esac

echo "media.env: ready (pwd=$PWD_NOW); run media-help for common commands"
